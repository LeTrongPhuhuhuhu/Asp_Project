<head>
    <link href="~/css/admin/suasanpham.css" rel="stylesheet" />
</head>
<div class="container_ssp">
    <h2>
        <a href="/Admin/Home/QuanLySanPham" class="back-to-product-management">Quản lý sản phẩm 6TL De Decor</a> &gt;
        <a href="/Admin/Home/ThemSanPham" class="add-product-link">Cập nhật sản phẩm</a>
    </h2>

    <form class="add-product-form_ssp">
        <div class="form-row_ssp">
            <input type="text" placeholder="Mã SP..." id="ma-sp_ssp">
            <input type="text" placeholder="Tên sản phẩm" id="ten-sp_ssp">
            <input type="text" placeholder="Giá bán" id="gia-ban_ssp">
            <input type="text" placeholder="Số lượng" id="so-luong_ssp">
        </div>
        <div class="form-row_ssp">
            <select id="danh-muc_ssp">
                <option>Danh mục</option>
                <option>Danh mục 1</option>
                <option>Danh mục 2</option>
            </select>
            <select id="trang-thai_ssp">
                <option>Chọn trạng thái</option>
                <option>Còn hàng</option>
                <option>Hết hàng</option>
            </select>
            <input type="text" placeholder="Nhà cung cấp" id="nha-cung-cap_ssp">
            <input type="text" placeholder="Giá vốn" id="gia-von_ssp">
        </div>
        <div class="form-row_ssp">
            <select id="chon-mau_ssp" onchange="updateColorPreview()">
                <option value="" disabled selected>Chọn màu</option>
                <option value="red" style="background-color: red; color: white;">Đỏ</option>
                <option value="blue" style="background-color: blue; color: white;">Xanh Dương</option>
                <option value="green" style="background-color: green; color: white;">Xanh Lá</option>
            </select>
            <input type="color" id="them-mau_ssp" title="Chọn màu mới">
            <button type="button" class="add-color-btn_ssp" onclick="addColor()">+</button>
            <input type="text" placeholder="Chất liệu" id="chat-lieu_ssp">
        </div>
        <div class="form-row_ssp">
            <label for="description_ssp">Mô tả</label>
            <textarea id="description_ssp" rows="5" placeholder="Này sử dụng ckeditor"></textarea>
        </div>
        <div class="upload-area_ssp">
            <label class="upload-label_ssp">
                <input type="file" id="file-upload_ssp" class="upload-input_ssp" accept="image/*" onchange="previewImage(event)">
                <div class="upload-content_ssp">
                    <img src="upload-icon.png" alt="Upload Icon" class="upload-icon_ssp" />
                    <p>Kéo thả hoặc chọn hình ảnh</p>
                </div>
                <img id="preview-image_ssp" class="preview-image_ssp" style="display: none;" alt="Image Preview">
            </label>
        </div>

        <div class="button-group_ssp">
            <button class="update-btn_tsp">Cập nhật</button>
            <button type="button" class="cancel-btn_ssp">Hủy</button>
        </div>

    </form>
</div>

<script>
    function previewImage(event) {
        const file = event.target.files[0];
        const preview = document.getElementById('preview-image_ssp');

        if (file) {
            const reader = new FileReader();
            reader.onload = function (e) {
                preview.src = e.target.result;
                preview.style.display = 'block';
            };
            reader.readAsDataURL(file);
        } else {
            preview.style.display = 'none';
        }
    }

    function addColor() {
        const selectElement = document.getElementById('chon-mau_ssp');
        const inputElement = document.getElementById('them-mau_ssp');
        const newColor = inputElement.value.trim();
        if (newColor !== '') {
            const newOption = document.createElement('option');
            newOption.value = newColor;
            newOption.textContent = newColor;
            newOption.style.backgroundColor = newColor;
            selectElement.appendChild(newOption);
            selectElement.value = newColor;
            inputElement.value = '';
        } else {
            alert('Vui lòng nhập màu hợp lệ.');
        }
    }
</script>
